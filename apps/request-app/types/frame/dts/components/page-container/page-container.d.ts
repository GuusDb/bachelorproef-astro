import { ReactElement, ReactNode } from 'react';
export declare type PageContainerProps = {
    children?: ReactNode;
};
export declare function PageContainer(props: PageContainerProps): ReactElement;
